/*
 * Frontend API configuration.
 *
 * Same-origin deployment (recommended):
 *   Leave this empty. The Node server serves both the API and this frontend.
 *
 * Separate frontend deployment (for example GitHub Pages):
 *   Replace the empty string with the public origin of the deployed backend:
 *   window.FITQUEST_API_URL = 'https://your-fitquest-backend.example.com';
 */
window.FITQUEST_API_URL = '';